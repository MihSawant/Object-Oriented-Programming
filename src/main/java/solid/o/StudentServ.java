package solid.o;

/* Created Interface StudentServ so that
next time if we add a method in
 interface then will implement it accordingly
 */
public interface StudentServ {
    void calculateAverageAttendance();

    void uploadEmailNewsletter();

    void coursesList();
}
